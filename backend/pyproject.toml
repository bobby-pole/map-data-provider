[project]
name = "map-data-provider-backend"
version = "0.1.0"
description = "Geospatial data processing worker for Map Data Provider."
requires-python = ">=3.14,<3.15"
dependencies = [
    "geopandas>=1.0.0",
    "osmnx>=2.0.0",
    "matplotlib>=3.8.0",
    "pyarrow>=20.0.0",
    "mapbox-vector-tile==2.2.0",
    "pmtiles==3.4.1",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.3,<9",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.4",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501", "E402"]
