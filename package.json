{
  "name": "map-data-quality-lab",
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "prepare": "pnpm run setup:git-hooks",
    "setup:git-hooks": "./scripts/setup_git_hooks.sh",
    "verify:pre-push": "./scripts/verify_pre_push.sh",
    "verify:provider": "./scripts/verify_provider.sh",
    "verify:node": "pnpm --dir backend-node run build && pnpm --dir backend-node test && pnpm --dir backend-node run lint",
    "verify:frontend": "pnpm --dir frontend test && pnpm --dir frontend run build && pnpm --dir frontend run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:fix": "pnpm --dir frontend run lint --fix && pnpm --dir backend-node run lint --fix"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
